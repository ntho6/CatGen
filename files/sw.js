var GHPATH = '/CatGen';

var APP_PREFIX = 'Cat Generator';

var VERSION = 'version_01';

var URLS = [    
  `${GHPATH}/`,
  `${GHPATH}/index.html`,
  `${GHPATH}/files/styles.css`,
  `${GHPATH}/files/script.js`
]
